# Python MCP SDK Migration Dependencies

# Official Python MCP SDK
mcp>=1.0.0

# Database connectivity (if not already installed)
asyncpg>=0.29.0

# Note: These should be added to the main requirements.txt file
# The official Python MCP SDK is actively maintained and should be 
# the primary dependency for MCP functionality going forward.

# To install:
# pip install mcp asyncpg

# For development:
# pip install mcp[dev] asyncpg
